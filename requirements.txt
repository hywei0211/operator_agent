# LLM 客户端
openai>=1.30.0
anthropic>=0.25.0
httpx>=0.27.0                  # Qwen (DashScope) HTTP 客户端

# 异步支持
asyncio-pool>=0.6.0

# 配置管理
pyyaml>=6.0.1
python-dotenv>=1.0.0

# 数据模型
pydantic>=2.7.0
dataclasses-json>=0.6.7

# GPU 工具（按需安装）
# torch>=2.3.0                  # PyTorch（用于参考实现和验证）
# triton>=3.0.0                 # OpenAI Triton（跨平台内核）
# cupy-cuda12x>=13.0.0          # CuPy（CUDA Python绑定）

# LoRA 训练
transformers>=4.40.0            # Hugging Face Transformers
peft>=0.11.0                    # Parameter-Efficient Fine-Tuning (LoRA)

# 测试
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# 代码质量
ruff>=0.4.0

# 工具库
tqdm>=4.66.0
rich>=13.7.0                   # 美化输出
click>=8.1.7                   # CLI支持
